
export const typeDRule = "@types.ts应该修改为@type.d.ts,并且放在typings文件中";
export const typeDeclareRule = '@types.ts 这种文件，只用来declare，不可以出现import <spe>';
export const nodeRule = '.node.ts 或者 .web.ts类型应该定义为*#node.ts 与 *#web.ts';
export const indexRule = 'index.ts只允许作为入口文件';